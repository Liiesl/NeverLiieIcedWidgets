[package]
name = "lazy-icon-test"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
iced = { version = "0.14", features = ["default", "advanced", "image", "svg"] }
neverliie-iced-widgets = { path = "../.." }
