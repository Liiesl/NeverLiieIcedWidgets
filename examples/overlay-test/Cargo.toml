[package]
name = "overlay-test"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
iced = { path = "../../iced", features = ["default", "advanced"] }
never-lie-iced-widgets = { path = "../.." }
